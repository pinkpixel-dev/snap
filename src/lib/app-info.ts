import pkg from "../../package.json";

export const APP_NAME = pkg.name || "snap";
export const APP_VERSION = pkg.version || "0.1.0";
export const APP_DESCRIPTION = pkg.description || "Fast, satisfying desktop image toolbox";
